@model IEnumerable<KDDongHo.Models.HANG_SX>

@*@{
        ViewBag.Title = "Index";
        Layout = "~/Views/Admin_Shared/_Layout.cshtml";
    }

    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TEN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LOGO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GIOITHIEU)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TEN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LOGO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GIOITHIEU)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }

    </table>*@

@{
    Layout = "~/Views/Admin_Shared/_Layout.cshtml";
    ViewBag.Title = "Sản phẩm";
}

@section styles{
    @Styles.Render("~/Static/Admin/assets/vendors/custom/datatables/datatables.bundle.css")
}

@section subheader{
    <div class="m-subheader d-flex">
        <div class="d-flex align-items-center">
            <div class="mr-auto">
                <h3 class="m-subheader__title m-subheader__title--separator ">Danh sách hãng sản xuất</h3>
            </div>
        </div>
        <div class="m-portlet__nav-item ml-auto">
            <a href="@Url.Action("Create", "AdminBrand")" class="btn btn-accent m-btn m-btn--custom m-btn--pill m-btn--icon m-btn--air">
                <span>
                    <i class="la la-plus"></i>
                    <span>New record</span>
                </span>
            </a>
        </div>
    </div>
}
<div class="m-portlet m-portlet--mobile">
    <div class="m-portlet__body">
        <table class="table table-striped- table-bordered table-hover table-checkable" id="m_table_1">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Logo</th>
                    <th>Tên</th>
                    <th>Giới thiệu</th>
                    <th>Điều chỉnh</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @item.ID
                        </td>
                        <td>
                            <img src="@item.LOGO" width="120"/>
                        </td>
                        <td>
                            @item.TEN
                        </td>
                        <td>
                            @item.GIOITHIEU
                        </td>
                        <td>
                            <a href="@Url.Action("Edit", "AdminBrand", new { id = item.ID })" class="btn m-btn btn-primary">
                                <i class="la la-edit"></i>
                            </a>
                            <a href="#" data-url="@Url.Action("Delete", "AdminBrand", new { id = item.ID })" data-toggle="modal" data-target="#delete_confirm"
                               class="delete_confirm btn m-btn btn-danger">
                                <i class="la la-trash"></i>
                            </a>
                            @*@Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.ID })*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@RenderPage("~/Views/Admin_Shared/Partials/_modal.cshtml")

@section scripts{
    @Scripts.Render("~/Static/Admin/assets/vendors/custom/datatables/datatables.bundle.js")
    @Scripts.Render("~/Static/Admin/assets/app/js/my-script.js")
    <script>
        $('#m_table_1').DataTable();
    </script>
}

